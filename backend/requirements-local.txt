# DiaIntel — Local Development Requirements (Python 3.11, Windows)
# Uses relaxed version pins for Windows + Python 3.11 wheel compatibility.
# The Docker container uses the pinned versions in requirements.txt.

# Core Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Database
sqlalchemy>=2.0.23
psycopg2-binary>=2.9.9
alembic>=1.13.0

# Redis
redis>=5.0.1

# Data Ingestion
zstandard>=0.22.0

# NLP / ML
spacy>=3.7.0
transformers>=4.36.0
tokenizers>=0.15.0

torch>=2.1.0
--extra-index-url https://download.pytorch.org/whl/cu121

# Scheduling
apscheduler>=3.10.0

# Knowledge Graph
networkx>=3.2.0

# HTTP & WebSocket
websockets>=12.0
python-multipart>=0.0.6

# Utilities
httpx>=0.25.0
orjson>=3.9.0
tqdm>=4.66.0
langdetect>=1.0.9
numpy<2
